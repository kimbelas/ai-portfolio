# Encryption

Files are encrypted with AES-256 at rest and protected in transit with TLS 1.3.

Encryption keys are derived from your password and never leave your device (a zero-knowledge design), so no one else can decrypt your files.

Business plans may optionally use customer-managed encryption keys (bring-your-own-key) for extra control.
