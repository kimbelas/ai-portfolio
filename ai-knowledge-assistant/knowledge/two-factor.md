# Two-Factor Authentication

Two-factor authentication (2FA) uses TOTP authenticator apps such as Google Authenticator or 1Password.

SMS-based 2FA is not supported. When you enable 2FA you receive one-time backup codes to store safely.

2FA is optional on all plans but required for Business plan administrators. Enable it under Settings → Security.
