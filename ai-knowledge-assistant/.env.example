# Copy this file to `.env` and fill in your real key.
# `.env` is gitignored — never commit real keys.

# Groq — FREE, no credit card. Get one at https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Added later — leave blank for now:
# (rung 3 uses FREE local embeddings, so no key needed there)
# ANTHROPIC_API_KEY=   # optional, only if you want to compare providers in evals
